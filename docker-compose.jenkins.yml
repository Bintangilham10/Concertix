services:
  backend:
    image: concertix-backend:jenkins
    # Jenkins runs Docker commands from inside a container. Removing the local
    # source bind mount keeps Docker Desktop from resolving /workspace/backend
    # as a host path during the runtime deploy stage.
    volumes: !reset []

  frontend:
    image: concertix-frontend:jenkins
